# 首页免责声明 - 功能开发文档

> 生成时间：2026-05-07
> 基于项目：resource-shop（免费图书分享小程序）
> 优先级：P3

---

## 1. 开发上下文

**技术栈**：Taro 4 + React 18 + TypeScript + SCSS + NutUI React Taro
**关联模块**：首页（`src/pages/index/index`）
**现有 UI 规范**：参考 `docs/ui-ux-spec.md`，辅助文字颜色 `$color-text-secondary`（#8C8CA1），间距 `$spacing-*`

---

## 2. 验收标准

- [ ] 首页底部（分类网格下方）显示免责声明区域
- [ ] 包含声明文字：本小程序仅供学习交流，资源来源于互联网，如有侵权请联系删除
- [ ] 包含联系邮箱 823088131@qq.com
- [ ] 样式与现有 UI 风格一致（小字、浅色、居中）
- [ ] 不影响首页现有功能（搜索、分类、最近浏览、精选书单）

---

## 3. 开发方案

### 3.1 数据模型改动

无。

### 3.2 API 改动

无。

### 3.3 UI 改动

- 修改页面：首页（`src/pages/index/index`）— 在分类网格下方新增免责声明文字区域
- 复用样式：使用 `variables.scss` 中的 `$color-text-secondary`、`$spacing-*`

**免责声明结构**：

```
┌──────────────────────────────────┐
│         （分类网格区域）            │
│                                  │
│  ─────────────────────────────── │
│  本小程序仅供学习交流              │
│  资源来源于互联网                  │
│  如有侵权请联系 823088131@qq.com  │
└──────────────────────────────────┘
```

### 3.4 文件清单

- 修改：`src/pages/index/index.tsx` — 在分类网格后新增免责声明区块
- 修改：`src/pages/index/index.scss` — 新增免责声明样式

### 3.5 新增依赖

无。

---

## 4. 实施步骤

**步骤 1：UI 层**
- [ ] 修改 `src/pages/index/index.tsx`：在 `index-page__categories` 区域后、`</>` 闭合标签前，新增免责声明 `View` 区块
- [ ] 修改 `src/pages/index/index.scss`：新增免责声明样式（小字号、浅色文字、居中、上下留白）

**步骤 2：验收检查**
- [ ] 逐条对照「验收标准」确认全部通过
- [ ] 验证搜索模式切换后免责声明正确隐藏（搜索模式下不显示）

---

## 5. 文档更新清单

**CHANGELOG.md**（在 `## [Unreleased]` 下添加）：

```markdown
### Added
- **免责声明**：首页底部新增版权免责声明和联系邮箱
  - 相关文件：`src/pages/index/index`
```

**README.md 更新清单**：

不需要更新。

**CLAUDE.md 更新清单**：

不需要更新。

---

## 6. 约束与风险

- **兼容性**：纯 UI 层改动，不涉及数据模型和 API，向后完全兼容
- **样式**：仅在非搜索模式下显示，不影响搜索结果区域
