## 场景描述

CONFIGS_URL 这个配置应该是多余的，其中的数据为
```JSON
{
    "军事": "http://qn-cdn.windlliu.com/books-tidy/configs/军事.json",
}
```

完全可以用 `CDN_BASE/{类型}.json` 来取代。

## 想要的效果

删除 CONFIGS_URL 这个配置参数，需要获取类型下的书籍时使用 `CDN_BASE/{类型}.json` 来替代